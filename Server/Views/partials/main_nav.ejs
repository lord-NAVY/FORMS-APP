<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
<a class="navbar-brand" href="/home"><i class="fab fa-symfony fa-lg"></i> &nbsp;Survey Factory</a>  
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
      <% if (title == "Survey Factory") { %>
       <a class="nav-link active" href="/home"><i class="fas fa-home fa-lg"></i> Home</a>
       <% } else { %>
        <a class="nav-link" href="/home"><i class="fas fa-home fa-lg"></i> Home</a>
       <% }  %> 
       <li class="nav-item">        
        </li>

      <% if(typeof displayName == 'string' && displayName) { %> 
      <% if (title == "Surveys") { %>
      <a class="nav-link active" href="/Survey"><i class="fas fa-poll fa-lg"></i> Surveys</a>
       <% } else { %>
       <a class="nav-link" href="/Survey"><i class="fas fa-poll fa-lg"></i> Surveys</a>
       <% }  %> 
        <li class="nav-item">
          
        </li>
      <% } %> 

        <% if(typeof displayName == 'string' && displayName) { %> 
      <li class="navbar-text"><i class="fas fa-user-alt fa-lg"></i> Welcome, <%= displayName %> </li>
      <li class="nav-item"> 
        <a href="/logout" class="nav-link">
        <i class="fas fa-lg fa-sign-out-alt fa-lg"></i> Logout</a>
      </li>
      <% } else { %>
        <li class="nav-item"> <a href="/login" class="nav-link">
          <i class="fas fa-lg fa-sign-in-alt fa-lg"></i> Login</a>
        </li>
        <% } %> 
      </ul>      
    </div>
  </div>
</nav>